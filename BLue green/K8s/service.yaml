apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  type: LoadBalancer  # Exposes externally via ELB
  ports:
  - port: 80
    targetPort: 5000
  selector:
    app: flask-app
    version: blue  # Initially points to blue; change to 'green' to switch