apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask-app
      version: green
  template:
    metadata:
      labels:
        app: flask-app
        version: green
    spec:
      containers:
      - name: flask-app
        image: your-repo/flask-app:green  # Replace with your image
        ports:
        - containerPort: 5000
        env:
        - name: APP_VERSION
          value: green